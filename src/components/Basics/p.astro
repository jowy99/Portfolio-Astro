---
const { props, reverse } = Astro.props;
---



{reverse ? (
    <>
        <p class={`${props} text-white prose dark:prose-invert leading-5 font-md dark:text-green_light-500`}>
            <slot/>
        </p>
    </>
) : (
    <>
        <p class={`${props} text-carbon-400 prose dark:prose-invert leading-5 font-md dark:text-green_light-500`}>
            <slot/>
        </p>
    </>
)}